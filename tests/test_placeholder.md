# Tests

Unit and integration tests — to be implemented.

Planned:
- test_event_queue.cpp — enqueue, recover, compact
- test_hal_sim.cpp — fault injection paths
- test_ota_state.cpp — slot selection, rollback
